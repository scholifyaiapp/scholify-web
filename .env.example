# Scholify — environment variables
# Copy this file to ".env" and fill in your real Supabase credentials.
# Without them the app runs in local DEMO mode (auth still works, no backend).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
