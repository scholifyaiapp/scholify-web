/*
 * LW-GLOBAL's own content contract, following the pattern BT, MA and FA established.
 *
 * ── Why LW needed this most of all ─────────────────────────────
 * LW shared the F1–F4 contract, which asserted 350 Section A items and 350 linked
 * Section B questions. For LW-Global both were fictions, and the paper was the worst
 * in the library:
 *
 *   · The bank was 48 authored questions padded with 294 permuted glossary prompts —
 *     so the diagnostic was largely trivia and the readiness score measured almost
 *     nothing. GLOBAL IS THE DEFAULT LW VARIANT, so that was the default experience.
 *   · Section B was 350 generated linked questions whose stems read "In review 7,
 *     situation 2 at Grove Ltd, which syllabus concept should be applied?", at one to
 *     two marks apiece — when the real exam has FIVE multi-task questions of SIX marks.
 *   · The syllabus was taught in eight chapters totalling ~1,150 words, generated by
 *     looping over glossary terms, with the whole CISG disposed of in a few sentences.
 *
 * ── The one thing that makes LW different from the other three ──
 * LW's real Section A is 25 questions of 2 marks PLUS 20 of 1 mark — 45 questions for
 * 70 marks. Every other rebuilt paper is uniform (FA is all 2s) or nearly so, and BT is
 * the only other paper that mixes. So this contract has to police a MARK MIX rather
 * than a single value: a bank of only 2-mark questions can reach 70 marks but never in
 * the real exam's shape, and three disjoint forms need roughly 60 one-mark and 75
 * two-mark authored questions to be composable.
 */

export const LW_GLOBAL_CONTENT_TARGET = {
  /** Authored, exam-standard Section A questions. Derived drills do not count. */
  authoredBank: 444,
  /** Study chapters in the reading tree, one per syllabus sub-topic group. */
  chapters: 33,
  /** Official syllabus areas the chapters must cover between them. */
  syllabusAreas: 8,
  /**
   * Authored Section B multi-task questions. Three mocks × five MTQs = 15, so this is
   * exactly enough for three DISJOINT sittings at the real unit size.
   */
  mtqUnits: 15,
  /** Marks per MTQ in the real LW exam. */
  mtqMarks: 6,
  /** Section A marks in the real exam: 25 × 2 plus 20 × 1. */
  sectionAMarks: 70,
  /** Section B marks in the real exam: 5 MTQs × 6. */
  sectionBMarks: 30,
  mockForms: 3,
  /** LW's exam is 100% objective test — there is no constructed-response section. */
  writtenQuestions: 0,
  /**
   * The real Section A's composition. Both values are examinable facts about the paper,
   * and together they are why the bank must hold both mark values.
   */
  sectionATwoMarkQuestions: 25,
  sectionAOneMarkQuestions: 20,
  /**
   * Floors on each mark value, sized for three disjoint forms: 3 × 20 one-mark and
   * 3 × 25 two-mark. Below either floor the paper cannot compose three real-shaped
   * sittings, however large the bank is in total.
   */
  minOneMarkQuestions: 60,
  minTwoMarkQuestions: 75,
  /**
   * LW is a LAW paper. Nothing in it is computed, so a numeric-entry question would be
   * measuring the wrong skill — and the contract test asserts there are none, which is
   * the opposite of the assertion FA's and MA's contracts make.
   */
  numericQuestions: 0,
} as const
